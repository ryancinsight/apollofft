# External Comparators

This directory is intentionally ignored by git so local performance and parity comparisons can vendor external tools without polluting the repository history.

Expected optional layout:

- `external/rustfft/`
- `external/pyfftw/`
- `external/results/`

Apollo must compile and test without any of these directories present.

